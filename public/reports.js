const RC = ['#6c5ce7', '#23c69a', '#f5a623', '#4c7df0', '#e5716a', '#8a7bf0', '#14a37a', '#b9becc'];
function _el(id) { return document.getElementById(id); }
function _empty(el) { if (el) { const p = document.createElement('div'); p.className = 'chart-empty'; p.textContent = 'No data yet'; el.parentNode.replaceChild(p, el); } }
function chPie(id, d) { const el = _el(id); if (!el) return; if (!d.labels.length) return _empty(el); new Chart(el, { type: 'doughnut', data: { labels: d.labels, datasets: [{ data: d.values, backgroundColor: RC, borderWidth: 0 }] }, options: { responsive: true, maintainAspectRatio: false, cutout: '58%', plugins: { legend: { position: 'bottom', labels: { boxWidth: 12, font: { size: 11 } } } } } }); }
function chBar(id, d, horiz, color) { const el = _el(id); if (!el) return; if (!d.labels.length) return _empty(el); new Chart(el, { type: 'bar', data: { labels: d.labels, datasets: [{ data: d.values, backgroundColor: color || '#6c5ce7', borderRadius: 6 }] }, options: { indexAxis: horiz ? 'y' : 'x', responsive: true, maintainAspectRatio: false, plugins: { legend: { display: false } }, scales: { x: { beginAtZero: true, grid: { display: !horiz ? false : true } }, y: { beginAtZero: true, grid: { display: horiz ? false : true } } } } }); }
function chLine(id, d) { const el = _el(id); if (!el) return; if (!d.labels.length) return _empty(el); new Chart(el, { type: 'line', data: { labels: d.labels, datasets: [{ data: d.values, borderColor: '#6c5ce7', backgroundColor: 'rgba(108,92,231,.12)', fill: true, tension: .3, pointRadius: 3, pointBackgroundColor: '#6c5ce7' }] }, options: { responsive: true, maintainAspectRatio: false, plugins: { legend: { display: false } }, scales: { y: { beginAtZero: true } } } }); }
function chGrouped(id, labels, series) { const el = _el(id); if (!el) return; new Chart(el, { type: 'bar', data: { labels: labels, datasets: series.map((s, i) => ({ label: s.label, data: s.data, backgroundColor: RC[i], borderRadius: 5 })) }, options: { responsive: true, maintainAspectRatio: false, plugins: { legend: { position: 'bottom' } }, scales: { y: { beginAtZero: true } } } }); }
